<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=, initial-scale=1.0">
    <title>5G Mapper - Login</title>
    <style>
        :root {
            --main-color: rgb(190, 202, 22);
            font-family: 'Courier New', Courier, monospace;
        }

        body {
            background-color: rgb(99, 99, 99);
            display: flex;
            height: 100vh;
            width: 100vw;
            align-items: center;
            justify-content: center;
        }

        form {
            text-align: center;
            font-size: large;
            width: 60vh;
            height: 50vh;
            border-radius: 10px;
            background-color: teal;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            row-gap: 1rem;
        }

        input {
            padding: 1em;
        }

        button {
            all: unset;
            border-radius: 12px;
            padding: 0.4rem 1rem;
            background-color: var(--main-color);
            transition: transform 400ms;
            cursor: pointer;
            padding: 1em 2em;
            font-size: 1.2rem;
            font-weight: bold;
        }

        button:hover {
            transform: scale(103%)
        }


        .error {
            animation: fadeIn 1s;
            border-radius: 12px;
            border: 1px solid red;
            padding: 0.5rem;
            background-color: white;
        }

        .error:before {
            content: "❌"

        }


        .footer {
            position: absolute;
            bottom: 0;
            right: 0;
            text-align: center;
            padding: 1rem;
            background-color: teal;
            color: white;

            a {
                color: white;
                text-decoration: none;
            }
        }

        @keyframes fadeIn {
            0% {
                opacity: 0;
            }

            100% {
                opacity: 1;
            }

        }
    </style>

</head>

<body>




    <form method="post" action="/login">
        <img src="favicon.png" alt="favicon" width="50px" height="50px" />
        <h2>Login (5G visualizer)</h2>
        <label>Username:</label>
        <input name="username" type="text" />
        <label>Password:</label>
        <input name="password" type="password" />
        <% if(error) { %>
            <div class="error">
                <%= error %>
            </div>
            <%} %>
                <button> Login</button>

                <footer class="footer">
                    <a href="https://espaciosoa.com/">&#169; EspacioSOA</a>
                    <!-- <p> Julian Camarillo 26, 6, 28037 Madrid</p> -->
                    <!-- <pre>Developed by <a href="https://alejandro-rey.me" >@alreylz</a></pre> -->
                </footer>

    </form>



</body>

</html>